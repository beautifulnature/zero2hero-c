#include <stdio.h>

int main(){
  int myArray[] = {1, 2, 3};
  for(int i = 0; i < sizeof(myArray); i++){
    printf("array element at %d is %d", i, myArray[i]);
  }

  printf("array element at position 0 through pointer\n", *myArray);
  return 0;
}
